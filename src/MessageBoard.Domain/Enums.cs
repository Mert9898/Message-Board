using System;

namespace MessageBoard.Domain
{
    public enum Theme { Light, Dark, HighContrast }
    public enum BadgeType { Helper, Expert, ModeratorStar, FoundingMember }
    public enum PostType { Text, Link, Image }
}
